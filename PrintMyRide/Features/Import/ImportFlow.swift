import CoreLocation

extension StravaService {
    func buildRouteFromActivity(from activity: StravaService.StravaActivity) async throws -> GPXRoute? {
        // Placeholder - throws until OAuth is configured
        throw PMRError.notConnected
    }
}